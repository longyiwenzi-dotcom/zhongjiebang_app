spring:
  datasource:
    url: jdbc:h2:mem:zhongjiebang;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
  main:
    banner-mode: off
logging:
  level:
    root: WARN
